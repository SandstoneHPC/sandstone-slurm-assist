'use strict';

describe('oide.slurm module', function() {

  beforeEach(module('oide.slurm'));

  describe('slurm controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var slurmCtrl = $controller('SlurmCtrl');
      expect(slurmCtrl).toBeDefined();
    }));

  });
});
